[project]
name = "kpidash-client"
version = "1.1.0"
description = "kpidash client daemon and CLI for Linux and Windows"
requires-python = ">=3.11"
dependencies = [
    "khlenv>=0.1.0",
    "psutil>=6",
    "pynvml>=11",
    "redis>=5",
    "GitPython>=3.1",
    "click>=8",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "ruff>=0.4",
]

[project.scripts]
kpidash-client = "kpidash_client.cli:main"

# khlenv is published to the homelab package store, not PyPI.
[[tool.uv.index]]
name = "homelab"
url = "https://kubsdb.encke-wahoo.ts.net:4880/simple/"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "N", "B"]

[tool.pytest.ini_options]
testpaths = ["tests"]
