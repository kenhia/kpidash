/* mockup_main.c — stub for Phase 1 (T002).
 * Full implementation lands in T040.
 * Built only when NOT TESTS_ONLY AND NOT CMAKE_CROSSCOMPILING. */
#include <stdio.h>

int main(void) {
    fprintf(stderr, "kpidash-mockup: stub — to be implemented in T040\n");
    return 0;
}
